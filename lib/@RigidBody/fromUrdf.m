function [root, name] = fromUrdf(file)
%   YOUR TODO LIST: Similar to scripts/tree_urdf_parse.m, parse a URDF file.
%       This time, however, set RigidBody properties Name, Parent, A, I, and M to 
%       their appropriate values.  You must use the class's set method to
%       set these values.
end